// Add this to the processes table to include archive button
// Replace the table actions section in loadProcesses function

// UPDATED CODE FOR PROCESS ACTIONS (line ~430-437 in crm-app.js):
/*
<td class="table-actions">
  <button class="btn btn-sm btn-secondary" onclick='editProcess(${JSON.stringify(p).replace(/'/g, "&apos;")})'>
    <i class="fas fa-edit"></i>
  </button>
  ${p.status !== 'Arquivado' && currentUser.role === 'admin' ? `
    <button class="btn btn-sm" style="background: #f59e0b; color: white;" onclick="archiveProcess(${p.id})" title="Arquivar">
      <i class="fas fa-archive"></i>
    </button>
  ` : ''}
  <button class="btn btn-sm btn-danger" onclick="deleteProcess(${p.id})">
    <i class="fas fa-trash"></i>
  </button>
</td>
*/

// The archiveProcess function is already defined in crm-users.js
